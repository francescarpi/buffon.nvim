---
on: [push, pull_request]
name: default

jobs:
  tests:
    name: tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Install dependencies
        run: sudo apt-get install -y nvim

      # - name: Nvim Busted Action
      #   uses: nvim-neorocks/nvim-busted-action@v1.1.0

      - name: Run test
        run: make
